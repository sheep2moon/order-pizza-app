import React from 'react';
import styled from 'styled-components';

const Cart = () => {
  return (
    <CartContainer>
      <h2>Cart Page</h2>
    </CartContainer>
  );
};

export default Cart;

const CartContainer = styled.div`
  margin-top: 4rem;
`;
